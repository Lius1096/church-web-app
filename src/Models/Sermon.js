class Sermon {
    constructor(title, preacher, date, scripture, audioUrl, videoUrl) {
      this.title = title;
      this.preacher = preacher;
      this.date = date;
      this.scripture = scripture;
      this.audioUrl = audioUrl;
      this.videoUrl = videoUrl;
    }
  }
  
  module.exports = Sermon;
  